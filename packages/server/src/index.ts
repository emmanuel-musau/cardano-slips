/**
 * The public entry point of `@cardano-slips/server`. Installing it must not
 * pull in a React tree or a CBOR decoder; `test/dependencies.test.ts` enforces
 * that.
 */
export * from "./define-slip.js"

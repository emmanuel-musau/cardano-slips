/**
 * The public entry point of `@cardano-slips/flow`. The `exports` map exposes
 * this module and nothing else, so moving a file is never a breaking change.
 *
 * Two properties hold as the rest of the package arrives. It refuses but does
 * not decide — the comparison is `verifier`'s, and a mismatch renders as a
 * block with no override path to write. And it does not own the page it is
 * dropped into: no fixed positioning, no assumptions about the document, and
 * colours only from the custom properties in `tokens.css`.
 */
export {}

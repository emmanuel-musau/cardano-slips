/**
 * The example Slips. Private to the workspace: the server tests, the client
 * tests and the docs read them, and nothing ships them to a publisher.
 */
export * from "./delegate.js"
export * from "./tip.js"

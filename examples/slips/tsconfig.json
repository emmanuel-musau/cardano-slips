{
  "$schema": "http://json.schemastore.org/tsconfig",

  // Solution file — owns no sources; `tsc -b` walks the package from here. Only
  // composite projects can be referenced, so the test config is absent by necessity.
  "extends": "../../tsconfig.base.json",
  "include": [],
  "references": [{ "path": "./tsconfig.src.json" }]
}

---
---

Repository tooling only: `packages/verifier/scripts/collect-fixture.mjs` adds a fixture from a transaction id and is never published — the package's `files` are `dist` and `src` — so nothing anyone installs changes.
